'use client';
import React from 'react';

/**
 * TEMPORARY STUB:
 *  - 競合/構文崩れでビルドが止まっているため、一時的にスタブ化して通します。
 *  - 本PRをマージ後に、バックアップから該当関数を段階的に戻します。
 */

export default function UnifiedKanban() {
  return (
    <div style={{ padding: 16 }}>
      <h1>Unified Kanban (Temporary Stub)</h1>
      <p>一時的にスタブ化しています。PRマージ後に実装を戻します。</p>
    </div>
  );
}
